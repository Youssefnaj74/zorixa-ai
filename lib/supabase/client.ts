/**
 * Browser Supabase client — use for client-side auth (e.g. signOut).
 * Same implementation as `browser.ts`; this entry matches common naming.
 */
export { createSupabaseBrowserClient } from "./browser";
