/**
 * Product feature toggles — flip to re-enable after review / compliance work.
 */

/** Public Explore prompts gallery (`/explore-prompts`). Off while catalog is reviewed for Dodo. */
export const EXPLORE_PROMPTS_PUBLIC = false;
