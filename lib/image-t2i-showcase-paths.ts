/** Local showcase PNG under public/ (generated when recipes are filled). */
export function imageT2iShowcaseOutputPath(composerModelId: string): string {
  return `/image-showcases/t2i/${composerModelId}.png`;
}
